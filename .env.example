# Core auth
JWT_SECRET=change_me

# Frontend URL used by backend for redirects/links
FRONTEND_URL=http://localhost:5173
APP_BASE_URL=http://localhost:5173

# SMTP (required in production for email verification + password reset delivery)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=
